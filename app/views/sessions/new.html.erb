<div class="depot-form">
	<% if flash[:alert] %>
		<p id="notice"><%= flash[:alert] %></p>
	<% end %>

	<%= form_tag do %>
		<fieldset>
			<legend>Зарегуйтесь, пожл</legend>

			<div>
				<%= label_tag :name, 'Имя:' %>
				<%= text_field_tag :name, params[:name] %>
			</div>

			<div>
				<%= label_tag :password, 'Пароль:' %>
				<%= password_field_tag :password, params[:password] %>
			</div>

			<div>
				<%= submit_tag "Login" %>
			</div>
		</fieldset>
	<% end %>
</div>


